import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter.app.noticias.victor',
  appName: 'noticias',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
